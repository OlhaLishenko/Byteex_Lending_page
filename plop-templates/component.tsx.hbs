import * as styles from './{{pascalCase name}}.module.scss';

export default function {{pascalCase name}}() {
  return (
    <div className={styles.{{camelCase name}} }>
      {{pascalCase name}}
    </div>
  );
}
